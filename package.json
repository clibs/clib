{
  "name": "clib",
  "version": "1.11.2",
  "repo": "clibs/clib",
  "install": "make clean install",
  "scripts": {
    "version": "which node >/dev/null && ./scripts/update-clib-json && git add clib.json"
  }
}
